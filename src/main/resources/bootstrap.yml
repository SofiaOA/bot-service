spring:
  application:
    name: bot-service

---
spring:
  profiles: test
  cloud:
    kubernetes:
      enabled: false
  autoconfigure:
    exclude: org.springframework.cloud.kubernetes.KubernetesAutoConfiguration