server:
  port: 4081

hedvig.product-pricing.url: 
    
management:
  security:
    enabled: false

hedvig:

spring:
  jpa:
    generate-ddl: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        hbm2ddl:
       #   auto: update
      javax:
        persistence:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-target: create.sql
    hibernate:
      ddl-auto: none
      show_sql: true
      format_sql: true
      naming_strategy: org.hibernate.cfg.ImprovedNamingStrategy

  jackson:
    serialization:
      write_dates_as_timestamps: false


  datasource:
    url: jdbc:postgresql://localhost:5432/bot-service
    username: postgres
    password: iambatman
    platform: POSTGRESQL
